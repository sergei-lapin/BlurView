rootProject.name = "BlurView"

include(":sample")

void includeProject(final String name, final String path) {
    include(name)
    final def projectDir = new File(rootDir, path)
    project(name).projectDir = projectDir
}

includeProject(":blurview", "lib")
